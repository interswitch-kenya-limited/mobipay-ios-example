# Uncomment the next line to define a global platform for your project
platform:ios, '10.0'

target 'frameworktest' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  # pod 'mobpay', :git => 'https://github.com/interswitch-kenya-limited/mobpay-ios-lib.git', :branch => 'makeCardTokenPayment'
  pod 'mobpay', :path => '../mobpay-ios'
  pod 'Eureka'
  # Pods for frameworktest

  target 'frameworktestTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'frameworktestUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
